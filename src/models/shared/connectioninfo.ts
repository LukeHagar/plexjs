/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Connection, Connection$inboundSchema } from "./connection.js";

export type ConnectionInfo = {
  accessToken?: string | undefined;
  clientIdentifier?: string | undefined;
  connection?: Array<Connection> | undefined;
  name?: string | undefined;
};

/** @internal */
export const ConnectionInfo$inboundSchema: z.ZodType<ConnectionInfo, unknown> =
  z.object({
    accessToken: types.optional(types.string()),
    clientIdentifier: types.optional(types.string()),
    Connection: types.optional(z.array(Connection$inboundSchema)),
    name: types.optional(types.string()),
  }).transform((v) => {
    return remap$(v, {
      "Connection": "connection",
    });
  });

export function connectionInfoFromJSON(
  jsonString: string,
): SafeParseResult<ConnectionInfo, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ConnectionInfo$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ConnectionInfo' from JSON`,
  );
}

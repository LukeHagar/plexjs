/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Display mode for the collection.
 */
export enum CollectionMode {
  Default = "default",
  HideItems = "hideItems",
  ShowItems = "showItems",
}
/**
 * Display mode for the collection.
 */
export type CollectionModeOpen = OpenEnum<typeof CollectionMode>;

/**
 * A collection of related media items.
 */
export type Collection = {
  /**
   * Blur hash for collection art.
   */
  artBlurHash?: string | undefined;
  /**
   * Whether the smart collection filter is based on the current user.
   */
  collectionFilterBasedOnUser?: boolean | undefined;
  /**
   * Display mode for the collection.
   */
  collectionMode?: CollectionModeOpen | undefined;
  /**
   * Whether the collection is published to Plex Discover.
   */
  collectionPublished?: boolean | undefined;
  /**
   * Sort order for items in the collection.
   */
  collectionSort?: string | undefined;
  /**
   * Timestamp of the last user rating.
   */
  lastRatedAt?: number | undefined;
  /**
   * Blur hash for collection thumbnail.
   */
  thumbBlurHash?: string | undefined;
  /**
   * User star rating (0-10).
   */
  userRating?: number | undefined;
};

/** @internal */
export const CollectionMode$inboundSchema: z.ZodType<
  CollectionModeOpen,
  unknown
> = openEnums.inboundSchema(CollectionMode);

/** @internal */
export const Collection$inboundSchema: z.ZodType<Collection, unknown> = z
  .object({
    artBlurHash: types.optional(types.string()),
    collectionFilterBasedOnUser: types.optional(types.boolean()),
    collectionMode: types.optional(CollectionMode$inboundSchema),
    collectionPublished: types.optional(types.boolean()),
    collectionSort: types.optional(types.string()),
    lastRatedAt: types.optional(types.number()),
    thumbBlurHash: types.optional(types.string()),
    userRating: types.optional(types.number()),
  });

export function collectionFromJSON(
  jsonString: string,
): SafeParseResult<Collection, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Collection$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Collection' from JSON`,
  );
}

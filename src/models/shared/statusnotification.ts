/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Server status notification (e.g. library scan complete)
 */
export type StatusNotification = {
  title?: string | undefined;
  description?: string | undefined;
  type?: string | undefined;
};

/** @internal */
export const StatusNotification$inboundSchema: z.ZodType<
  StatusNotification,
  unknown
> = z.object({
  title: types.optional(types.string()),
  description: types.optional(types.string()),
  type: types.optional(types.string()),
});

export function statusNotificationFromJSON(
  jsonString: string,
): SafeParseResult<StatusNotification, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => StatusNotification$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'StatusNotification' from JSON`,
  );
}

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The state of this queue
 *
 * @remarks
 *   - deciding: At least one item is still being decided
 *   - waiting: At least one item is waiting for transcode and none are currently transcoding
 *   - processing: At least one item is being transcoded
 *   - done: All items are available (or potentially expired)
 *   - error: At least one item has encountered an error
 */
export enum DownloadQueueStatus {
  Deciding = "deciding",
  Waiting = "waiting",
  Processing = "processing",
  Done = "done",
  Error = "error",
}
/**
 * The state of this queue
 *
 * @remarks
 *   - deciding: At least one item is still being decided
 *   - waiting: At least one item is waiting for transcode and none are currently transcoding
 *   - processing: At least one item is being transcoded
 *   - done: All items are available (or potentially expired)
 *   - error: At least one item has encountered an error
 */
export type DownloadQueueStatusOpen = OpenEnum<typeof DownloadQueueStatus>;

export type DownloadQueue = {
  id?: number | undefined;
  itemCount?: number | undefined;
  /**
   * The state of this queue
   *
   * @remarks
   *   - deciding: At least one item is still being decided
   *   - waiting: At least one item is waiting for transcode and none are currently transcoding
   *   - processing: At least one item is being transcoded
   *   - done: All items are available (or potentially expired)
   *   - error: At least one item has encountered an error
   */
  status?: DownloadQueueStatusOpen | undefined;
};

/** @internal */
export const DownloadQueueStatus$inboundSchema: z.ZodType<
  DownloadQueueStatusOpen,
  unknown
> = openEnums.inboundSchema(DownloadQueueStatus);

/** @internal */
export const DownloadQueue$inboundSchema: z.ZodType<DownloadQueue, unknown> = z
  .object({
    id: types.optional(types.number()),
    itemCount: types.optional(types.number()),
    status: types.optional(DownloadQueueStatus$inboundSchema),
  });

export function downloadQueueFromJSON(
  jsonString: string,
): SafeParseResult<DownloadQueue, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DownloadQueue$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DownloadQueue' from JSON`,
  );
}

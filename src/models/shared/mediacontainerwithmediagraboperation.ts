/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  MediaGrabOperation,
  MediaGrabOperation$inboundSchema,
} from "./mediagraboperation.js";

/**
 * `MediaContainer` is the root element of most Plex API responses. It serves as a generic container for various types of content (Metadata, Hubs, Directories, etc.) and includes pagination information (offset, size, totalSize) when applicable.
 *
 * @remarks
 * Common attributes: - identifier: Unique identifier for this container - size: Number of items in this response page - totalSize: Total number of items available (for pagination) - offset: Starting index of this page (for pagination)
 * The container often "hoists" common attributes from its children. For example, if all tracks in a container share the same album title, the `parentTitle` attribute may appear on the MediaContainer rather than being repeated on each track.
 */
export type MediaContainerWithMediaGrabOperationMediaContainer = {
  identifier?: string | undefined;
  /**
   * The offset of where this container page starts among the total objects available. Also provided in the `X-Plex-Container-Start` header.
   */
  offset?: number | undefined;
  size?: number | undefined;
  /**
   * The total size of objects available. Also provided in the `X-Plex-Container-Total-Size` header.
   */
  totalSize?: number | undefined;
  mediaGrabOperation?: Array<MediaGrabOperation> | undefined;
};

export type MediaContainerWithMediaGrabOperation = {
  mediaContainer?:
    | MediaContainerWithMediaGrabOperationMediaContainer
    | undefined;
};

/** @internal */
export const MediaContainerWithMediaGrabOperationMediaContainer$inboundSchema:
  z.ZodType<MediaContainerWithMediaGrabOperationMediaContainer, unknown> = z
    .object({
      identifier: types.optional(types.string()),
      offset: types.optional(types.number()),
      size: types.optional(types.number()),
      totalSize: types.optional(types.number()),
      MediaGrabOperation: types.optional(
        z.array(MediaGrabOperation$inboundSchema),
      ),
    }).transform((v) => {
      return remap$(v, {
        "MediaGrabOperation": "mediaGrabOperation",
      });
    });

export function mediaContainerWithMediaGrabOperationMediaContainerFromJSON(
  jsonString: string,
): SafeParseResult<
  MediaContainerWithMediaGrabOperationMediaContainer,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      MediaContainerWithMediaGrabOperationMediaContainer$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'MediaContainerWithMediaGrabOperationMediaContainer' from JSON`,
  );
}

/** @internal */
export const MediaContainerWithMediaGrabOperation$inboundSchema: z.ZodType<
  MediaContainerWithMediaGrabOperation,
  unknown
> = z.object({
  MediaContainer: types.optional(
    z.lazy(() =>
      MediaContainerWithMediaGrabOperationMediaContainer$inboundSchema
    ),
  ),
}).transform((v) => {
  return remap$(v, {
    "MediaContainer": "mediaContainer",
  });
});

export function mediaContainerWithMediaGrabOperationFromJSON(
  jsonString: string,
): SafeParseResult<MediaContainerWithMediaGrabOperation, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      MediaContainerWithMediaGrabOperation$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MediaContainerWithMediaGrabOperation' from JSON`,
  );
}

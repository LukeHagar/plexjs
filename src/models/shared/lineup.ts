/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * - `-1`: N/A
 *
 * @remarks
 * - `0`: Over the air
 * - `1`: Cable
 * - `2`: Satellite
 * - `3`: IPTV
 * - `4`: Virtual
 */
export enum LineupType {
  Minus1 = -1,
  Zero = 0,
  One = 1,
  Two = 2,
  Three = 3,
  Four = 4,
}
/**
 * - `-1`: N/A
 *
 * @remarks
 * - `0`: Over the air
 * - `1`: Cable
 * - `2`: Satellite
 * - `3`: IPTV
 * - `4`: Virtual
 */
export type LineupTypeOpen = OpenEnum<typeof LineupType>;

export type Lineup = {
  title?: string | undefined;
  /**
   * The type of this object (`lineup` in this case)
   */
  type?: string | undefined;
  /**
   * - `-1`: N/A
   *
   * @remarks
   * - `0`: Over the air
   * - `1`: Cable
   * - `2`: Satellite
   * - `3`: IPTV
   * - `4`: Virtual
   */
  lineupType?: LineupTypeOpen | undefined;
  location?: string | undefined;
  /**
   * The uuid of this lineup
   */
  uuid?: string | undefined;
};

/** @internal */
export const LineupType$inboundSchema: z.ZodType<LineupTypeOpen, unknown> =
  openEnums.inboundSchemaInt(LineupType);

/** @internal */
export const Lineup$inboundSchema: z.ZodType<Lineup, unknown> = z.object({
  title: types.optional(types.string()),
  type: types.optional(types.string()),
  lineupType: types.optional(LineupType$inboundSchema),
  location: types.optional(types.string()),
  uuid: types.optional(types.string()),
});

export function lineupFromJSON(
  jsonString: string,
): SafeParseResult<Lineup, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Lineup$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Lineup' from JSON`,
  );
}

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  DecisionResult,
  DecisionResult$inboundSchema,
} from "./decisionresult.js";
import {
  TranscodeSession,
  TranscodeSession$inboundSchema,
} from "./transcodesession.js";

/**
 * The state of the item:
 *
 * @remarks
 *   - deciding: The item decision is pending
 *   - waiting: The item is waiting for transcode
 *   - processing: The item is being transcoded
 *   - available: The item is available for download
 *   - error: The item encountered an error in the decision or transcode
 *   - expired: The transcoded item has timed out and is no longer available
 */
export enum DownloadQueueItemStatus {
  Deciding = "deciding",
  Waiting = "waiting",
  Processing = "processing",
  Available = "available",
  Error = "error",
  Expired = "expired",
}
/**
 * The state of the item:
 *
 * @remarks
 *   - deciding: The item decision is pending
 *   - waiting: The item is waiting for transcode
 *   - processing: The item is being transcoded
 *   - available: The item is available for download
 *   - error: The item encountered an error in the decision or transcode
 *   - expired: The transcoded item has timed out and is no longer available
 */
export type DownloadQueueItemStatusOpen = OpenEnum<
  typeof DownloadQueueItemStatus
>;

/**
 * The transcode session object which is not yet documented otherwise it'd be a $ref here.
 */
export type Transcode = {};

export type DownloadQueueItem = {
  decisionResult?: DecisionResult | undefined;
  /**
   * The error encountered in transcoding or decision
   */
  error?: string | undefined;
  id?: number | undefined;
  key?: string | undefined;
  queueId?: number | undefined;
  /**
   * The state of the item:
   *
   * @remarks
   *   - deciding: The item decision is pending
   *   - waiting: The item is waiting for transcode
   *   - processing: The item is being transcoded
   *   - available: The item is available for download
   *   - error: The item encountered an error in the decision or transcode
   *   - expired: The transcoded item has timed out and is no longer available
   */
  status?: DownloadQueueItemStatusOpen | undefined;
  /**
   * The transcode session object which is not yet documented otherwise it'd be a $ref here.
   */
  transcode?: Transcode | undefined;
  /**
   * The transcode session if item is currently being transcoded
   */
  transcodeSession?: TranscodeSession | undefined;
};

/** @internal */
export const DownloadQueueItemStatus$inboundSchema: z.ZodType<
  DownloadQueueItemStatusOpen,
  unknown
> = openEnums.inboundSchema(DownloadQueueItemStatus);

/** @internal */
export const Transcode$inboundSchema: z.ZodType<Transcode, unknown> = z.object(
  {},
);

export function transcodeFromJSON(
  jsonString: string,
): SafeParseResult<Transcode, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Transcode$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Transcode' from JSON`,
  );
}

/** @internal */
export const DownloadQueueItem$inboundSchema: z.ZodType<
  DownloadQueueItem,
  unknown
> = z.object({
  DecisionResult: types.optional(DecisionResult$inboundSchema),
  error: types.optional(types.string()),
  id: types.optional(types.number()),
  key: types.optional(types.string()),
  queueId: types.optional(types.number()),
  status: types.optional(DownloadQueueItemStatus$inboundSchema),
  transcode: types.optional(z.lazy(() => Transcode$inboundSchema)),
  TranscodeSession: types.optional(TranscodeSession$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "DecisionResult": "decisionResult",
    "TranscodeSession": "transcodeSession",
  });
});

export function downloadQueueItemFromJSON(
  jsonString: string,
): SafeParseResult<DownloadQueueItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DownloadQueueItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DownloadQueueItem' from JSON`,
  );
}

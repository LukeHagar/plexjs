/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * Describes both the purpose and intended presentation of the image.
 */
export enum ImageType {
  Background = "background",
  BackgroundSquare = "backgroundSquare",
  Banner = "banner",
  ClearLogo = "clearLogo",
  CoverPoster = "coverPoster",
  Snapshot = "snapshot",
}
/**
 * Describes both the purpose and intended presentation of the image.
 */
export type ImageTypeOpen = OpenEnum<typeof ImageType>;

/**
 * Images such as movie posters and background artwork are represented by Image elements.
 *
 * @remarks
 */
export type Image = {
  /**
   * Describes both the purpose and intended presentation of the image.
   */
  type?: ImageTypeOpen | undefined;
  /**
   * Title to use for accessibility.
   */
  alt?: string | undefined;
  /**
   * The relative path or absolute url for the image.
   */
  url?: string | undefined;
};

/** @internal */
export const ImageType$inboundSchema: z.ZodType<ImageTypeOpen, unknown> =
  openEnums.inboundSchema(ImageType);

/** @internal */
export const Image$inboundSchema: z.ZodType<Image, unknown> = z.object({
  type: types.optional(ImageType$inboundSchema),
  alt: types.optional(types.string()),
  url: types.optional(types.string()),
});

export function imageFromJSON(
  jsonString: string,
): SafeParseResult<Image, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Image$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Image' from JSON`,
  );
}

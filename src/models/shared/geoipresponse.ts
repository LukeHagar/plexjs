/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

export type GeoIPResponse = {
  city?: string | undefined;
  coordinates?: string | undefined;
  country?: string | undefined;
  countryCode?: string | undefined;
  subdivisions?: string | undefined;
  timezone?: string | undefined;
};

/** @internal */
export const GeoIPResponse$inboundSchema: z.ZodType<GeoIPResponse, unknown> = z
  .object({
    city: types.optional(types.string()),
    coordinates: types.optional(types.string()),
    country: types.optional(types.string()),
    country_code: types.optional(types.string()),
    subdivisions: types.optional(types.string()),
    timezone: types.optional(types.string()),
  }).transform((v) => {
    return remap$(v, {
      "country_code": "countryCode",
    });
  });

export function geoIPResponseFromJSON(
  jsonString: string,
): SafeParseResult<GeoIPResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GeoIPResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GeoIPResponse' from JSON`,
  );
}

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import * as shared from "../shared/index.js";

export type ListProvidersMediaContainer = {
  provider?: Array<shared.Provider> | undefined;
};

/**
 * Feature key identifier.
 */
export enum Key {
  Search = "search",
  Metadata = "metadata",
  Content = "content",
  Match = "match",
  Manage = "manage",
  Timeline = "timeline",
  Rate = "rate",
  Playqueue = "playqueue",
  Playlist = "playlist",
  Subscribe = "subscribe",
  Promoted = "promoted",
  Continuewatching = "continuewatching",
  Collection = "collection",
  Actions = "actions",
  Imagetranscoder = "imagetranscoder",
  QueryParser = "queryParser",
  Grid = "grid",
}
/**
 * Feature key identifier.
 */
export type KeyOpen = OpenEnum<typeof Key>;

/**
 * A feature supported by a media provider.
 */
export type ListProvidersResponse = {
  mediaContainer?: ListProvidersMediaContainer | undefined;
  /**
   * Human-readable feature title.
   */
  title?: string | undefined;
  /**
   * Feature type.
   */
  type?: string | undefined;
  /**
   * Feature key identifier.
   */
  key?: KeyOpen | undefined;
};

/** @internal */
export const ListProvidersMediaContainer$inboundSchema: z.ZodType<
  ListProvidersMediaContainer,
  unknown
> = z.object({
  Provider: types.optional(z.array(shared.Provider$inboundSchema)),
}).transform((v) => {
  return remap$(v, {
    "Provider": "provider",
  });
});

export function listProvidersMediaContainerFromJSON(
  jsonString: string,
): SafeParseResult<ListProvidersMediaContainer, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListProvidersMediaContainer$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListProvidersMediaContainer' from JSON`,
  );
}

/** @internal */
export const Key$inboundSchema: z.ZodType<KeyOpen, unknown> = openEnums
  .inboundSchema(Key);

/** @internal */
export const ListProvidersResponse$inboundSchema: z.ZodType<
  ListProvidersResponse,
  unknown
> = z.object({
  MediaContainer: types.optional(
    z.lazy(() => ListProvidersMediaContainer$inboundSchema),
  ),
  title: types.optional(types.string()),
  type: types.optional(types.string()),
  key: types.optional(Key$inboundSchema),
}).transform((v) => {
  return remap$(v, {
    "MediaContainer": "mediaContainer",
  });
});

export function listProvidersResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListProvidersResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListProvidersResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListProvidersResponse' from JSON`,
  );
}

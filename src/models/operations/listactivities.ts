/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v4";
import { remap as remap$ } from "../../lib/primitives.js";
import { safeParse } from "../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import * as types from "../../types/primitives.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import * as shared from "../shared/index.js";

export type ListActivitiesMediaContainer = {
  activity?: Array<shared.Activity> | undefined;
};

/**
 * OK
 */
export type ListActivitiesResponse = {
  mediaContainer?: ListActivitiesMediaContainer | undefined;
};

/** @internal */
export const ListActivitiesMediaContainer$inboundSchema: z.ZodType<
  ListActivitiesMediaContainer,
  unknown
> = z.object({
  Activity: types.optional(z.array(shared.Activity$inboundSchema)),
}).transform((v) => {
  return remap$(v, {
    "Activity": "activity",
  });
});

export function listActivitiesMediaContainerFromJSON(
  jsonString: string,
): SafeParseResult<ListActivitiesMediaContainer, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListActivitiesMediaContainer$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListActivitiesMediaContainer' from JSON`,
  );
}

/** @internal */
export const ListActivitiesResponse$inboundSchema: z.ZodType<
  ListActivitiesResponse,
  unknown
> = z.object({
  MediaContainer: types.optional(
    z.lazy(() => ListActivitiesMediaContainer$inboundSchema),
  ),
}).transform((v) => {
  return remap$(v, {
    "MediaContainer": "mediaContainer",
  });
});

export function listActivitiesResponseFromJSON(
  jsonString: string,
): SafeParseResult<ListActivitiesResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ListActivitiesResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ListActivitiesResponse' from JSON`,
  );
}

/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { HTTPClient } from "./http.js";
import { Logger } from "./logger.js";
import { RetryConfig } from "./retries.js";
import { Params, pathToFunc } from "./url.js";

/**
 * Contains the list of servers available to the SDK
 */
export const ServerList = [
  "https://{IP-description}.{identifier}.plex.direct:{port}",
  "{protocol}://{host}:{port}",
  "https://{server_url}",
] as const;

export type SDKOptions = {
  apiKey?: string | (() => Promise<string>) | undefined;

  /**
   * Allows setting the xPlexClientIdentifier parameter for all supported operations
   */
  xPlexClientIdentifier?: string | undefined;

  /**
   * Allows setting the xPlexProduct parameter for all supported operations
   */
  xPlexProduct?: string | undefined;

  /**
   * Allows setting the xPlexVersion parameter for all supported operations
   */
  xPlexVersion?: string | undefined;

  /**
   * Allows setting the xPlexPlatform parameter for all supported operations
   */
  xPlexPlatform?: string | undefined;

  /**
   * Allows setting the xPlexPlatformVersion parameter for all supported operations
   */
  xPlexPlatformVersion?: string | undefined;

  /**
   * Allows setting the xPlexDevice parameter for all supported operations
   */
  xPlexDevice?: string | undefined;

  /**
   * Allows setting the xPlexModel parameter for all supported operations
   */
  xPlexModel?: string | undefined;

  /**
   * Allows setting the xPlexDeviceVendor parameter for all supported operations
   */
  xPlexDeviceVendor?: string | undefined;

  /**
   * Allows setting the xPlexDeviceName parameter for all supported operations
   */
  xPlexDeviceName?: string | undefined;

  /**
   * Allows setting the xPlexMarketplace parameter for all supported operations
   */
  xPlexMarketplace?: string | undefined;

  httpClient?: HTTPClient;
  /**
   * Allows overriding the default server used by the SDK
   */
  serverIdx?: number | undefined;
  /**
   * Sets the identifier variable for url substitution
   */
  identifier?: string | undefined;
  /**
   * Sets the IP-description variable for url substitution
   */
  ipDescription?: string | undefined;
  /**
   * Sets the port variable for url substitution
   */
  port?: string | undefined;
  /**
   * Sets the protocol variable for url substitution
   */
  protocol?: string | undefined;
  /**
   * Sets the host variable for url substitution
   */
  host?: string | undefined;
  /**
   * Sets the server_url variable for url substitution
   */
  serverUrl?: string | undefined;
  /**
   * Allows overriding the default server URL used by the SDK
   */
  serverURL?: string | undefined;
  /**
   * Allows overriding the default user agent used by the SDK
   */
  userAgent?: string | undefined;
  /**
   * Allows overriding the default retry config used by the SDK
   */
  retryConfig?: RetryConfig;
  timeoutMs?: number;
  debugLogger?: Logger;
};

export function serverURLFromOptions(options: SDKOptions): URL | null {
  let serverURL = options.serverURL;

  const serverParams: Params[] = [
    {
      "identifier": options.identifier ?? "0123456789abcdef0123456789abcdef",
      "IP-description": options.ipDescription ?? "1-2-3-4",
      "port": options.port ?? "32400",
    },
    {
      "protocol": options.protocol ?? "http",
      "host": options.host ?? "localhost",
      "port": options.port ?? "32400",
    },
    {
      "server_url": options.serverUrl ?? "http://localhost:32400",
    },
  ];
  let params: Params = {};

  if (!serverURL) {
    const serverIdx = options.serverIdx ?? 0;
    if (serverIdx < 0 || serverIdx >= ServerList.length) {
      throw new Error(`Invalid server index ${serverIdx}`);
    }
    serverURL = ServerList[serverIdx] || "";
    params = serverParams[serverIdx] || {};
  }

  const u = pathToFunc(serverURL)(params);
  return new URL(u);
}

export const SDK_METADATA = {
  language: "typescript",
  openapiDocVersion: "1.1.1",
  sdkVersion: "0.41.0",
  genVersion: "2.723.11",
  userAgent: "speakeasy-sdk/typescript 0.41.0 2.723.11 1.1.1 @lukehagar/plexjs",
} as const;
